- The quiz app is a portion of an app which prints out the users details and scores from a quiz.

- The app gives you the privilege to edit name and email.

- To run the app:

	-  open the .js file directly with a text editor that have a console feature and run the file there.[Recommended]

	- if your text editor doesn't have have a console feature, open the app.html file in your browser and navigate to the browser console.